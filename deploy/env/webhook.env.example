# Webhook ingress — merge with docs/env/shared.env.example
# MONGODB_URI must be the SAME database as Hub (routes events by x_connections)
# Railway: create a service, config file /docs/railway/webhook/railway.toml, generate a public domain.

# Railway sets PORT automatically.
# PORT=3001

# Must match the host registered for incoming X webhooks (no trailing slash)
WEBHOOK_PUBLIC_BASE_URL=http://localhost:3001
# WEBHOOK_PUBLIC_BASE_URL=https://${{Webhook.RAILWAY_PUBLIC_DOMAIN}}

# CRC + POST signatures — must match Hub X_API_KEY_SECRET (OAuth 1.0 API Key Secret).
# Prefer X_API_KEY_SECRET (same Railway shared var as Hub). Do not set X_CLIENT_SECRET here.
X_API_KEY_SECRET=
# Optional alias (only if not using X_API_KEY_SECRET):
# X_CONSUMER_SECRET=
