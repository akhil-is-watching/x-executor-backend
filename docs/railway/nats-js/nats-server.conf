# NATS for x-executor (JetStream). Mount a Railway volume at /data for persistence.
port: 4222
http_port: 8222
server_name: x-executor-nats

jetstream {
  store_dir: /data
  max_mem: 256MB
  max_file: 1GB
}
